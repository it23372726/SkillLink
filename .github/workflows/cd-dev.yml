name: CD-Dev
on:
  workflow_dispatch: {}   # manual only (disabled for push/PR)

jobs:
  deploy-api:
    if: ${{ false }}      # hard-off even if triggered manually (remove in Sprint 2)
    runs-on: ubuntu-latest
    steps: []

  deploy-frontend:
    if: ${{ false }}      # hard-off
    runs-on: ubuntu-latest
    steps: []

  migrate-db:
    if: ${{ false }}      # hard-off
    runs-on: ubuntu-latest
    steps: []
